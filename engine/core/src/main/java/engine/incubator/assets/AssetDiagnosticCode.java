package engine.incubator.assets;

public enum AssetDiagnosticCode {
    GROUP_QUEUED,
    GROUP_LOADED,
    GROUP_UNLOADED,
    FALLBACK_SELECTED,
    LOAD_FAILED,
    TYPE_MISMATCH,
    STALE_HANDLE
}
